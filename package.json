{
  "name": "mla-monorepo",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "front:lint": "npm --prefix frontend run lint",
    "back:lint": "make -C backend format-check lint",
    "back:format": "make -C backend format"
  },
  "devDependencies": {
    "husky": "^9.0.0"
  },
  "dependencies": {
    "@swc/helpers": "^0.5.18",
    "tslib": "^2.8.1"
  }
}
