diff --git a/frontend/app/app.vue b/frontend/app/app.vue
index 8e7f33e..ac434f4 100644
--- a/frontend/app/app.vue
+++ b/frontend/app/app.vue
@@ -2,6 +2,7 @@
   <div>
     <NuxtLayout>
       <NuxtPage />
+       <UNotifications /> 
     </NuxtLayout>
   </div>
 </template>
diff --git a/frontend/layers/auth/app/middleware/01.auth.global.ts b/frontend/layers/auth/app/middleware/01.auth.global.ts
index b3c5828..b9f124a 100644
--- a/frontend/layers/auth/app/middleware/01.auth.global.ts
+++ b/frontend/layers/auth/app/middleware/01.auth.global.ts
@@ -20,7 +20,7 @@ export default defineNuxtRouteMiddleware(async (to) => {
       await authStore.fetchMe()
     } else {
       // Si expiré, on nettoie tout de suite
-      await authStore.logout(false)
+      await authStore.logout(false, to.path)
     }
   }
 
diff --git a/frontend/layers/auth/app/stores/useAuthStore.ts b/frontend/layers/auth/app/stores/useAuthStore.ts
index 32495c0..e6ee3af 100644
--- a/frontend/layers/auth/app/stores/useAuthStore.ts
+++ b/frontend/layers/auth/app/stores/useAuthStore.ts
@@ -2,8 +2,6 @@ import { defineStore } from 'pinia'
 import type { AuthUser } from '../repositories/AuthRepository'
 
 export const useAuthStore = defineStore('auth', () => {
-  const { $api } = useNuxtApp()
-  const route = useRoute()
 
   // --- ÉTAT (STATE) ---
   const cookieOptions = { maxAge: 60 * 60 * 24 * 7, path: '/' }
@@ -27,6 +25,8 @@ export const useAuthStore = defineStore('auth', () => {
   // --- ACTIONS ---
 
   async function login(credentials: Record<'username' | 'password', string>) {
+      const { $api } = useNuxtApp()
+
     const response = await $api.auth.login(credentials)
 
     token.value = response.token
@@ -51,6 +51,8 @@ export const useAuthStore = defineStore('auth', () => {
    * Récupération des infos profil depuis le serveur
    */
   async function fetchMe(): Promise<void> {
+      const { $api } = useNuxtApp()
+
     if (!token.value) return
 
     try {
@@ -70,7 +72,9 @@ export const useAuthStore = defineStore('auth', () => {
   /**
    * Déconnexion complète
    */
-  async function logout(shouldRedirect = true): Promise<void> {
+  async function logout(shouldRedirect = true, currentPath?:string): Promise<void> {
+      const { $api } = useNuxtApp()
+
     if (token.value) {
       await $api.auth.logout().catch(() => {})
     }
@@ -79,7 +83,7 @@ export const useAuthStore = defineStore('auth', () => {
     user.value = null
     expiresAt.value = null
 
-    if (shouldRedirect && route.path !== '/login') {
+    if (shouldRedirect && currentPath !== '/login') {
       await navigateTo('/login')
     }
   }
diff --git a/frontend/layers/base/app/composables/useApiFetch.ts b/frontend/layers/base/app/composables/useApiFetch.ts
index 1db4869..3d9c8f8 100644
--- a/frontend/layers/base/app/composables/useApiFetch.ts
+++ b/frontend/layers/base/app/composables/useApiFetch.ts
@@ -1,6 +1,7 @@
 import type { UseFetchOptions } from '#app'
 import { defu } from 'defu'
 import { useAuthStore } from '~~/layers/auth/app/stores/useAuthStore'
+import type { EnhancedApiError } from '../../types/api'
 
 export const useApiFetch = <ResT, TransformT = ResT>(
   url: string | (() => string),
@@ -10,6 +11,7 @@ export const useApiFetch = <ResT, TransformT = ResT>(
   const authStore = useAuthStore()
   const route = useRoute()
 
+  const { notifyError } = useErrorHandler()
   /**
    * Construction des headers dynamiques.
    * On récupère le token du store à chaque exécution.
@@ -44,30 +46,36 @@ export const useApiFetch = <ResT, TransformT = ResT>(
     /**
      * Intercepteur d'erreur global
      */
+   /**
+     * Intercepteur d'erreur global mis à jour
+     */
     onResponseError({ response, request }) {
       const isLoginRequest = request.toString().includes('/auth/token')
 
+      // 1. Gestion de la Session (401)
       if (response.status === 401 && !isLoginRequest) {
         authStore.logout()
-
         if (route.path !== '/login') {
           navigateTo({
             path: '/login',
             query: { redirect: route.fullPath },
           })
         }
+        // On ne notifie pas forcément pour un 401 car la redirection est explicite
+        return 
       }
 
-      if (response.status === 422) {
-        // console.error('[API Validation Error]:', response._data)
-      }
+      // 2. Notification Automatique (400, 403, 422, 500, etc.)
+      // Le notifyError gère déjà le verrouillage pour éviter les doublons
+      console.log("Notify error")
+      notifyError(response as unknown as EnhancedApiError)
 
+      // 3. Logique métier spécifique optionnelle
       if (response.status === 403) {
-        // console.error('[API Forbidden]: Droits insuffisants.')
+        // Logique spécifique si besoin (ex: redirection vers une page "Accès Refusé")
       }
     },
   }
-
   // defu fusionne les options.
   // Attention : l'ordre est important, 'options' (utilisateur) écrase 'defaults'
   const params = defu(options, defaults)
diff --git a/frontend/layers/base/app/repositories/BaseRepository.ts b/frontend/layers/base/app/repositories/BaseRepository.ts
index 32afa39..778739f 100644
--- a/frontend/layers/base/app/repositories/BaseRepository.ts
+++ b/frontend/layers/base/app/repositories/BaseRepository.ts
@@ -1,4 +1,5 @@
 import type { UseFetchOptions } from '#app'
+import type { EnhancedApiError } from '../../types/api'
 
 /**
  * BaseRepository - Classe abstraite pour centraliser la logique des services API
@@ -19,12 +20,18 @@ export abstract class BaseRepository {
     // On propage les deux génériques à useApiFetch pour autoriser le 'transform'
     const { data, error, refresh, pending } = await useApiFetch<ResT, TransformT>(url, options)
 
-    // Gestion centralisée de la remontée d'erreur vers le composant appelant
     if (error.value) {
-      // On throw l'erreur pour qu'elle puisse être catchée par le bloc try/catch du composant
-      throw error.value
+      /**
+       * CRITÈRE EXPERT : Normalisation de l'erreur
+       * On cast l'erreur Fetch en notre type EnhancedApiError.
+       * L'intercepteur dans useApiFetch a déjà déclenché le Toast,
+       * mais on "throw" pour permettre au composant de gérer sa logique locale (ex: stop loader).
+       */
+      const apiError = error.value as unknown as EnhancedApiError
+      
+      // On enrichit l'erreur si besoin avant de la lancer
+      throw apiError
     }
-
     return {
       // On garantit que data.value est bien du type TransformT
       data: data.value as TransformT,
diff --git a/frontend/package.json b/frontend/package.json
index 331faa8..545c2ff 100644
--- a/frontend/package.json
+++ b/frontend/package.json
@@ -16,7 +16,6 @@
     "@pinia/nuxt": "^0.11.3",
     "@shared/ui-core": "workspace:*",
     "lucide-vue-next": "^0.575.0",
-    "nuxt": "^4.3.1",
     "pinia": "^3.0.4",
     "vue": "^3.5.28",
     "vue-router": "^4.6.4"
@@ -25,6 +24,7 @@
     "@nuxt/eslint": "^1.15.1",
     "@nuxt/kit": "^4.3.1",
     "@nuxt/test-utils": "^4.0.0",
+    "@nuxt/ui": "2.18.2",
     "@tailwindcss/vite": "^4.2.0",
     "autoprefixer": "^10.4.24",
     "eslint": "^10.0.1",
@@ -32,6 +32,7 @@
     "eslint-plugin-unused-imports": "^4.4.1",
     "husky": "^9.1.7",
     "lint-staged": "^16.2.7",
+    "nuxt": "^4.3.1",
     "postcss": "^8.5.6",
     "prettier": "^3.8.1",
     "prettier-plugin-tailwindcss": "^0.7.2",
diff --git a/package.json b/package.json
index 460b0ec..2b430df 100644
--- a/package.json
+++ b/package.json
@@ -9,5 +9,9 @@
   },
   "devDependencies": {
     "husky": "^9.0.0"
+  },
+  "dependencies": {
+    "@swc/helpers": "^0.5.18",
+    "tslib": "^2.8.1"
   }
 }
